export enum GameType {
    WWTBAM
}

export enum GamePhase {
    lobby,
    prompt,
    gameover
}

export enum PlayerType {
    host,
    guest,
    audience
}

export enum Cheat {
    guestAnswer,
    audienceAnswer,
    splitAnswers
}

export enum AvatarEnum {
    LULE,
    KEKW,
    LULW,
    happE,
    flushE
}

export enum Badge {
    
}